export const publicRoutes = [
    "/",
    "/new-verification"
]

export const authRoutes = [
    "/login",
    "/signup",
    "/reset",
    "/auth-error",
    "/new-password"
]

export const apiPrefix = "/api/auth"

export const DEFAULT_LOGIN_REDIRECT = "/monitor"